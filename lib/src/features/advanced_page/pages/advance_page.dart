import 'package:flutter/material.dart';

class AndvancePage extends StatelessWidget {
  const AndvancePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text("Advance Page"),));
  }
}